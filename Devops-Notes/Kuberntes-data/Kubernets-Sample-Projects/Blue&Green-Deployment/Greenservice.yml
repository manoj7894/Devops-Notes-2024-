apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: my-nginx
    version: v2
    color: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort